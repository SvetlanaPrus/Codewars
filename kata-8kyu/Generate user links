// Generate user links: Your task is to create userlinks for the url, you will be given a username and must return a valid link.
//Example
//generate_link('matt c')
//http://www.codewars.com/users/matt%20c

// Second try:
function generateLink(user) {
  const encodeUser = encodeURIComponent(user);
  return `http://www.codewars.com/users/${encodeUser}`
}

// First try, fail:

function generateLink(user) {
  const link = "http://www.codewars.com/users/";
  let userNameWithSpace = ""
  
  if(user.includes(" ")){
    userNameWithSpace = user.split(" ").join("%20");
  }
  
  return user.includes(" ")? link+userNameWithSpace : link+user;
}
